# Dependencies (bundled into server/index.js by esbuild)
node_modules/

# Source code (compiled into server/index.js by build)
src/
scripts/

# VCS
.git/
.husky/
.github/
.worktrees/

# Build output (prevents recursive packing)
dist/

# Dev config files (not needed at runtime)
tsconfig.json
vitest.config.ts
eslint.config.mjs
pnpm-lock.yaml
.lintstagedrc.json
.npmrc
.nvmrc
.prettierrc
docs/
deepseek_mcp.log
*.tsbuildinfo
